<footer id="footer">
	<div class="outer">
	  <div id="footer-info">
		  <div class="footer-left">
			  <% if (theme.wbd){ %>
				  &copy; <%= theme.wbd.y %>-<%= date(new Date(), 'YYYY') %> <%= config.author || config.title %>
			  <% } else { %>
				  &copy; <%= date(new Date(), 'YYYY') %> <%= config.author || config.title %>
			  <% } %>
			  <% if (theme.beian){ %>
					<br><a href="https://beian.miit.gov.cn/" target="_blank"><%= theme.beian %></a>
			  <% } %>
		  </div>
			<div class="footer-right">
			<% if (theme.upyun){ %>
				  <a href="https://console.upyun.com/register/?invite=B1Tr4zsVf" target="_blank"> &nbsp; <img src="https://oss.xxwhite.com/static/upyun.png" width="100"></a>
			<% } %>
					欢迎你的来访
			</div> 
  
<!--《添加网站运行时间 -->
<!--<br/>-->
<a>⏰</a>
<span id="timeDate">载入天数...</span><span id="times">载入时分秒...</span>
<script>
	var now = new Date();
	
	function createtime() {
		var grt = new Date("04/30/2020 18:52:00"); //此处修改你的建站时间或者网站上线时间	月/日/年 | 时/分/秒
		now.setTime(now.getTime() + 250);
		days = (now - grt) / 1000 / 60 / 60 / 24;
		dnum = Math.floor(days);
		hours = (now - grt) / 1000 / 60 / 60 - (24 * dnum);
		hnum = Math.floor(hours);
		if (String(hnum).length == 1) {
			hnum = "0" + hnum;
		}
		minutes = (now - grt) / 1000 / 60 - (24 * 60 * dnum) - (60 * hnum);
		mnum = Math.floor(minutes);
		if (String(mnum).length == 1) {
			mnum = "0" + mnum;
		}
		seconds = (now - grt) / 1000 - (24 * 60 * 60 * dnum) - (60 * 60 * hnum) - (60 * mnum);
		snum = Math.round(seconds);
		if (String(snum).length == 1) {
			snum = "0" + snum;
		}
		document.getElementById("timeDate").innerHTML = " 本站已安全运行 " + dnum + " 天 ";
		document.getElementById("times").innerHTML = hnum + " 小时 " + mnum + " 分 " + snum + " 秒";
		}
	setInterval("createtime()", 250);
</script>
<a>🎉</a>
<!-- 添加网站运行时间》 -->

  <% if (theme.busuanzi && theme.busuanzi.enable){ %>
  <% } %>
  <!-- 访问量跟访客数 -->
<% if (theme.busuanzi && theme.busuanzi.enable){ %>
	<!-- 不蒜子统计 -->
	你是本博第<span id="busuanzi_value_site_pv"></span>位访客&nbsp;|
	历经 <span id="busuanzi_value_site_uv"></span> 次回眸才与你相遇
	<script async src="//busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js"></script>
  <% } %>
  <!-- end -->

<!-- 《网站备案号 -->
<% if (theme.icp && theme.icp.enable && theme.icp.id){ %>
    <div class="BeiAn-info">
        <a href="https://beian.miit.gov.cn/" style="color:#f72b07" target="_blank" title="备案号">
            <%- theme.icp.id %>
        </a>
    </div>
<% } %>
<!-- 网站备案号》 -->

</footer>